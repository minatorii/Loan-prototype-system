'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
  Schema = mongoose.Schema;

/**
 * Express Schema
 */
var ExpressSchema = new Schema({
  // Express model fields
  // ...
});

mongoose.model('Express', ExpressSchema);
